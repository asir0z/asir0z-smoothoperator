# WIN-2.5 scripts (placeholder)

Created during authorized execution.

| Script | Purpose |
|--------|---------|
| `install-win-2.5.ps1` | winget-based install of selected components |
| `configure-terminal.ps1` | Apply Windows Terminal settings from `../config/` |
| `configure-launcher.ps1` | Launcher hotkey + plugins |
| `configure-prompt.ps1` | Oh My Posh theme + `$PROFILE` hook |

All scripts must be idempotent and log actions for evidence capture.
