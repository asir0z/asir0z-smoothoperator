# Bare-metal profile — apply after dual-boot install (NOT active in VM)
#
# Host target (2026-07-23 inventory):
#   CPU   AMD Ryzen 5 7500F
#   RAM   32 GB
#   Disk  NVMe ~931 GB (dual-boot with Windows)
#   GPU   TBD — install mesa/vulkan or proprietary driver before enabling
#
# Before first Hyprland login on bare metal:
#   1. pacman -S mesa vulkan-radeon   OR   nvidia-open-dkms  (match GPU)
#   2. WORKSTATION_PROFILE=baremetal bash linux/arch-workstation/apply-config.sh
#   3. hyprctl reload
#
# No software-render overrides here — uses real GPU.
